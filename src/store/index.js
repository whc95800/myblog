import { createStore } from 'vuex'

export default createStore({
  state: {
  },
  mutations: {
    initRouter(state,data){
      state.router = data;
    }
  },
  actions: {
  },
  modules: {
  }
})
